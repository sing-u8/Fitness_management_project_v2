import { AbsNumberPipe } from './abs-number.pipe';

describe('AbsNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AbsNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
