import { RateOfChangePipe } from './rate-of-change.pipe';

describe('RateOfChangePipe', () => {
  it('create an instance', () => {
    const pipe = new RateOfChangePipe();
    expect(pipe).toBeTruthy();
  });
});
