import { WordSlicePipe } from './word-slice.pipe';

describe('WordSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new WordSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
